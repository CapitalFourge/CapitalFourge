[["SymbolAutocomplete`](frontend/components/trading/symbol-autocomplete.tsx)** - Already implements autocomplete with search suggestions\n- **[`TradeDialog`](frontend/components/trading/trade-dialog.tsx)** - Trading dialog for buy/sell operations\n- **[`Dashboard`](frontend/app/(dashboard)/dashboard/page.tsx)** - Main dashboard with asset search\n\n### GraphQL API\n- **`searchSymbols(query: String!, limit: Int): [AssetSuggestion]`** - Available in schema (line 87 of [`schema.graphqls`](portfolio-manager/src/main/resources/graphql/schema.graphqls:87))\n- Returns `AssetSuggestion` with `symbol` and `name` fields\n\n### Current Implementation Issues\n\n#### 1. TradeDialog - Buy Symbol Input (Lines 345-350)\n```tsx\n<Input\n    placeholder=", "AAPL, BTC, etc\"\n    value={symbol}\n    onChange={(e) => handleSymbolChange(e.target.value)}\n    className=\"bg-black/40 border-white/10 text-white\"\n/>\n```\n**Problem**: Plain text input - users must type full symbol from memory\n\n#### 2. TradeDialog - Sell Symbol Input (Lines 333-343)\n```tsx\n<select\n    value={symbol}\n    onChange={(e) => handleSymbolChange(e.target.value)}\n    className=\"w-full bg-black/40 border border-white/10 rounded-md p-2 text-sm text-white focus:outline-none focus:ring-1 focus:ring-slate-400\"\n>\n    <option value=\"", "Seleccionar activo...</option>\n    {currentPortfolioPositions.map((pos: any) => (\n        <option key={pos.symbol} value={pos.symbol}>{pos.symbol}</option>\n    ))}\n</select>\n```\n**Status**: Already uses select dropdown with portfolio positions \u2713\n\n#### 3. Dashboard Search Input (Lines 89-97)\n```tsx\n<Input\n    placeholder=\"BUSCAR_TICKER...\"\n    value={searchInput}\n    onChange={(e) => setSearchInput(e.target.value)}\n    className=\"bg-white/5 border-white/5 pl-11 pr-4 py-6 rounded-2xl w-full md:w-64 focus:ring-1 focus:ring-slate-400 transition-all text-xs font-bold tracking-widest", "Problem**: Plain text input - no suggestions while typing\n\n---\n\n## Implementation Plan\n\n### Task 1: Update TradeDialog - Buy Symbol with Autocomplete\n\n**File**: [`frontend/components/trading/trade-dialog.tsx`](frontend/components/trading/trade-dialog.tsx)\n\n**Changes**:\n1. Import `SymbolAutocomplete` component\n2. Replace the buy symbol input (lines 345-350) with `SymbolAutocomplete`\n3. Keep the existing `handleSymbolChange` logic for price fetching\n\n**Implementation Details**:\n```tsx\n// Add import\nimport { SymbolAutocomplete } from \"@/components/trading/symbol-autocomplete\";\n\n// Replace buy input (when type === \"buy\")\n<SymbolAutocomplete\n    value={symbol}\n    onChange={handleSymbolChange}\n    placeholder=", "AAPL, BTC, etc\"\n    disabled={type === \"sell"], ["frontend/app/(dashboard)/dashboard/page.tsx`](frontend/app/(dashboard)/dashboard/page.tsx)\n\n**Changes**:\n1. Import `SymbolAutocomplete` component\n2. Replace the search input (lines 89-97) with `SymbolAutocomplete`\n3. Update `handleSearch` to work with autocomplete selection\n\n**Implementation Details**:\n```tsx\n// Add import\nimport { SymbolAutocomplete } from \"@/components/trading/symbol-autocomplete\";\n\n// Replace search form\n<SymbolAutocomplete\n    value={searchInput}\n    onChange={(value) => {\n        setSearchInput(value);\n        if (value) {\n            setActiveSymbol(value.toUpperCase());\n        }\n    }}\n    placeholder=\"BUSCAR_TICKER...\"\n    className=\"w-full md:w-64", "Benefits**:\n- Real-time suggestions while typing\n- Users can discover assets they didn't know existed\n- Better UX for asset exploration\n\n---\n\n### Task 3: Enhance SymbolAutocomplete Component (Optional Improvements)\n\n**File**: [`frontend/components/trading/symbol-autocomplete.tsx`](frontend/components/trading/symbol-autocomplete.tsx)\n\n**Potential Enhancements**:\n1. Add keyboard navigation (already implemented \u2713)\n2. Add loading state indicator (already implemented \u2713)\n3. Add \"no results", "message (already implemented \u2713)\n4. Consider adding recent symbols or favorites\n\n**Current Status**: Component is well-implemented with:\n- Debounced search (300ms)\n- Keyboard navigation (Arrow keys, Enter, Escape)\n- Click outside to close\n- Loading spinner\n- Empty state handling\n\n---\n\n## Architecture Diagram\n\n```mermaid\ngraph TD\n    A[User Types Symbol] --> B[SymbolAutocomplete Component]\n    B --> C[Debounce 300ms]\n    C --> D[GraphQL Query: searchSymbols]\n    D --> E[Portfolio Manager Backend]\n    E --> F[AssetSearchService]\n    F --> G[Returns AssetSuggestion List]\n    G --> H[Display Suggestions Dropdown]\n    H --> I[User Selects Symbol]\n    I --> J[Update Parent Component State]\n    J --> K[Fetch Price & Update UI]\n```\n\n---\n\n## Component Flow\n\n### TradeDialog Buy Flow\n```mermaid\nsequenceDiagram\n    participant U as User\n    participant TD as TradeDialog\n    participant SA as SymbolAutocomplete\n    participant GQL as GraphQL API\n    participant PS as Price Service\n\n    U->>TD: Click \"COMPRAR\"\n    TD->>SA: Render SymbolAutocomplete\n    U->>SA: Type \"b\"\n    SA->>SA: Debounce 300ms\n    SA->>GQL: searchSymbols(query: \"b", "limit: 5)\n    GQL->>SA: Return [BTC, BNB, BSV, BCH, BAT]\n    SA->>U: Show suggestions\n    U->>SA: Select \"BTC\"\n    SA->>TD: onChange(\"BTC\")\n    TD->>PS: Fetch BTC price\n    PS->>TD: Return price\n    TD->>U: Display price\n```\n\n### Dashboard Search Flow\n```mermaid\nsequenceDiagram\n    participant U as User\n    participant D as Dashboard\n    participant SA as SymbolAutocomplete\n    participant GQL as GraphQL API\n    participant RT as Real-time Price\n\n    U->>D: Type \"a\"\n    D->>SA: Render SymbolAutocomplete\n    SA->>SA: Debounce 300ms\n    SA->>GQL: searchSymbols(query: \"a", "limit: 5)\n    GQL->>SA: Return [AAPL, AMD, AMZN, ADBE, ATVI]\n    SA->>U: Show suggestions\n    U->>SA: Select \"AAPL\"\n    SA->>D: onChange(\"AAPL\")\n    D->>D: setActiveSymbol(\"AAPL", "D->>RT: Subscribe to AAPL price\n    RT->>D: Stream real-time price\n    D->>U: Update chart & display\n```\n\n---\n\n## Files to Modify\n\n| File | Lines | Change Type |\n|------|-------|-------------|\n| [`frontend/components/trading/trade-dialog.tsx`](frontend/components/trading/trade-dialog.tsx) | 345-350 | Replace input with SymbolAutocomplete |\n| [`frontend/app/(dashboard)/dashboard/page.tsx`](frontend/app/(dashboard)/dashboard/page.tsx) | 89-97 | Replace input with SymbolAutocomplete |\n\n---\n\n## Testing Checklist\n\nAfter implementation, verify:\n\n- [ ] Buy dialog shows suggestions when typing\n- [ ] Buy dialog allows selecting from suggestions\n- [ ] Buy dialog fetches price after selection\n- [ ] Dashboard search shows suggestions when typing\n- [ ] Dashboard search updates active symbol on selection\n- [ ] Keyboard navigation works (Arrow keys, Enter, Escape)\n- [ ] Click outside closes suggestions\n- [ ] Loading state displays during search\n- [ ] Empty state shows when no results\n- [ ] Sell dialog still shows portfolio positions (no change needed)\n\n---\n\n## Edge Cases to Consider\n\n1. **Empty Portfolio**: When selling with no positions, the select should show \"No positions available", 2.0, "Network Error**: Handle GraphQL errors gracefully in autocomplete\n3. **Slow Connection**: Debounce already handles this (300ms)\n4. **Invalid Symbol**: User can still type invalid symbols if they don't select from suggestions\n5. **Case Sensitivity**: Autocomplete converts to uppercase (already implemented)\n\n---\n\n## Benefits Summary\n\n| Feature | Before | After |\n|---------|--------|-------|\n| Buy Symbol | Plain text input | Autocomplete with suggestions |\n| Dashboard Search | Plain text input | Autocomplete with suggestions |\n| Sell Symbol | Select dropdown (already good) | No change needed |\n| User Experience | Must remember symbols | Discover & select easily |\n| Error Rate | High (typos, invalid symbols) | Low (validated suggestions) |\n\n---\n\n## Questions for User\n\n1. **Sell Dialog**: The current sell implementation already uses a select dropdown showing portfolio positions. Do you want to keep this as-is, or would you prefer it to also use the autocomplete component (filtered to only show portfolio positions)?\n\n2. **Suggestion Limit**: Currently shows 5 suggestions. Would you like to increase this limit?\n\n3. **Recent Symbols**: Would you like to add a \"recently used symbols", "section to the autocomplete?\n\n4. **Styling**: The SymbolAutocomplete has a different style than the current inputs. Should we adjust the styling to match the existing design (glass effect, rounded corners, etc.)?\n\n---\n\n## Next Steps\n\nOnce you approve this plan, I will switch to Code mode to implement the changes:\n\n1. Update [`trade-dialog.tsx`](frontend/components/trading/trade-dialog.tsx) to use SymbolAutocomplete for buy\n2. Update [`dashboard/page.tsx`](frontend/app/(dashboard)/dashboard/page.tsx) to use SymbolAutocomplete for search\n3. Test the implementation\n4. Make any styling adjustments if needed"]]
