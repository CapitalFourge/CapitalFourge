spring:
  application:
    name: portfolio-manager

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:finsight_db}?sslmode=${DB_SSLMODE:disable}&preferQueryMode=simple&prepareThreshold=0
    username: ${DB_USER:user}
    password: ${DB_PASSWORD:password}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 4
      minimum-idle: 1
      idle-timeout: 300000
      connection-timeout: 20000
      data-source-properties:
        cachePrepStmts: false
        prepStmtCacheSize: 0
        prepStmtCacheSqlLimit: 0
        # Disable prepared statement caching at driver level
        prepareThreshold: 0
        useServerPrepStmts: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  redis:
    url: ${SPRING_REDIS_URL}
    ssl:
      enabled: false
    lettuce:
      pool:
        max-active: 4
        max-idle: 4
        min-idle: 0
      shutdown-timeout: 100ms
    client-type: lettuce

  graphql:
    graphiql:
      enabled: false
    path: /graphql

  data-collector:
    api-key: ${DATA_COLLECTOR_API_KEY:internal-service-key}
    base-url: ${DATA_COLLECTOR_BASE_URL:http://localhost:8000}

  report:
    generator:
      script-path: ${REPORT_GENERATOR_SCRIPT_PATH:report-service/generator.py}

  jwt:
    secret: ${JWT_SECRET}
    issuer: ${JWT_ISSUER:capital-fourge}
    access-expiration-ms: ${JWT_ACCESS_EXPIRATION_MS:86400000}
    refresh-expiration-ms: ${JWT_REFRESH_EXPIRATION_MS:604800000}

# gRPC disabled - we use REST to data-collector now
grpc:
  server:
    enabled: false
    port: -1

server:
  port: ${PORT:10000}
  address: 0.0.0.0

# Health check on main server (same port as app)
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,caches,prometheus
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
    prometheus:
      enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true
    redis:
      enabled: false
    caches:
      enabled: false
    db:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
    distribution:
      percentiles-histogram:
        http.server.requests: true
        http.client.requests: true
      sla:
        http.server.requests: 100ms,500ms,1s,5s
      percentiles:
        http.server.requests: 0.5,0.95,0.99

logging:
  level:
    root: INFO
    com.capitalfourge.portfoliomanager: DEBUG
    org.springframework.graphql: DEBUG
    org.springframework.web: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 30